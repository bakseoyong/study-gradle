/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package study.gradle.app;

import study.gradle.list.LinkedList;

import static study.gradle.utilities.StringUtils.join;
import static study.gradle.utilities.StringUtils.split;
import static study.gradle.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
